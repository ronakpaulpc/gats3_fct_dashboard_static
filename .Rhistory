) |>
# Categorize the PSUs as per num of PSUs completed
mutate(psu_status = case_when(
comp_hh_n == 33 ~ 1,
comp_hh_n %in% 30:32 ~ 2,
.default = 3
)) |>
# Convert to factor
mutate(psu_status = factor(
psu_status,
levels = c(1, 2, 3),
labels = c("Completed", "Partially Completed", "Not Completed")
)) |>
# Sort the table by Progress Status and Descending % of PSUs completed
arrange(psu_status, -pct_hh)
# View as gt table
df_tbl_progress_psu |>
# Remove columns we need not display
select(-c(prop_hh, target_col)) |>
gt() |>
# Bullet Plot
# gt_plt_bullet(column = pct_hh, target = target_col, width = 50) |>
# Progress Bar Plot
gt_plt_bar_pct(column = pct_hh, width = 150, labels = TRUE) |>
# Colour Code the PSU Progress Status
data_color(
columns = psu_status,
method = "factor",
palette = c("Completed" = "green4",
"Partially Completed" = "orange",
"Not Completed" = "red4")
) |>
# Apply column labels
cols_label(
"DATA02" = "PSU Code",
"dist_name" = "District Name",
"tot_hh_n" = "Total HHs",
"comp_hh_n" = "Completed HHs",
"pct_hh" = "Completion Rate",
"psu_status" = "PSU Status"
) |>
# Autofit table
tab_options(table.width = pct(100)) |>
# Apply table theme
gt_theme_espn()
# View as gt table
df_tbl_progress_psu |>
# Remove columns we need not display
select(-c(prop_hh, target_col)) |>
gt() |>
# Bullet Plot
# gt_plt_bullet(column = pct_hh, target = target_col, width = 50) |>
# Progress Bar Plot
gt_plt_bar_pct(column = pct_hh, height = 20, width = 150, labels = TRUE) |>
# Colour Code the PSU Progress Status
data_color(
columns = psu_status,
method = "factor",
palette = c("Completed" = "green4",
"Partially Completed" = "orange",
"Not Completed" = "red4")
) |>
# Apply column labels
cols_label(
"DATA02" = "PSU Code",
"dist_name" = "District Name",
"tot_hh_n" = "Total HHs",
"comp_hh_n" = "Completed HHs",
"pct_hh" = "Completion Rate",
"psu_status" = "PSU Status"
) |>
# Autofit table
tab_options(table.width = pct(100)) |>
# Apply table theme
gt_theme_espn()
# View as gt table
df_tbl_progress_psu |>
# Remove columns we need not display
select(-c(prop_hh, target_col)) |>
gt() |>
# Bullet Plot
# gt_plt_bullet(column = pct_hh, target = target_col, width = 50) |>
# Progress Bar Plot
gt_plt_bar_pct(column = pct_hh, font_size = 12px, width = 150, labels = TRUE) |>
# View as gt table
df_tbl_progress_psu |>
# Remove columns we need not display
select(-c(prop_hh, target_col)) |>
gt() |>
# Bullet Plot
# gt_plt_bullet(column = pct_hh, target = target_col, width = 50) |>
# Progress Bar Plot
gt_plt_bar_pct(
column = pct_hh, font_size = "12px",
width = 150, labels = TRUE) |>
# Colour Code the PSU Progress Status
data_color(
columns = psu_status,
method = "factor",
palette = c("Completed" = "green4",
"Partially Completed" = "orange",
"Not Completed" = "red4")
) |>
# Apply column labels
cols_label(
"DATA02" = "PSU Code",
"dist_name" = "District Name",
"tot_hh_n" = "Total HHs",
"comp_hh_n" = "Completed HHs",
"pct_hh" = "Completion Rate",
"psu_status" = "PSU Status"
) |>
# Autofit table
tab_options(table.width = pct(100)) |>
# Apply table theme
gt_theme_espn()
# View as gt table
df_tbl_progress_psu |>
# Remove columns we need not display
select(-c(prop_hh, target_col)) |>
gt() |>
# Bullet Plot
# gt_plt_bullet(column = pct_hh, target = target_col, width = 50) |>
# Progress Bar Plot
gt_plt_bar_pct(
column = pct_hh, font_size = "14px",
width = 150, labels = TRUE
) |>
# Colour Code the PSU Progress Status
data_color(
columns = psu_status,
method = "factor",
palette = c("Completed" = "green4",
"Partially Completed" = "orange",
"Not Completed" = "red4")
) |>
# Apply column labels
cols_label(
"DATA02" = "PSU Code",
"dist_name" = "District Name",
"tot_hh_n" = "Total HHs",
"comp_hh_n" = "Completed HHs",
"pct_hh" = "Completion Rate",
"psu_status" = "PSU Status"
) |>
# Autofit table
tab_options(table.width = pct(100)) |>
# Apply table theme
gt_theme_espn()
# ** District Completion Rate Table ---------------------------------------
# Transform the Data Overall for Interview Progress calculation Across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by PSUs
group_by(DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01)
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate overall stats of PSUs
summarize(
tot_psu_n = n(),
comp_psu_n = sum(progress_psu_01)
) |>
# Calculate Percentage of Completed PSUs
mutate(comp_psu_pct = (comp_psu_n / tot_psu_n)) |>
# Check Progress
data_progress_psu
# ** District Completion Rate Table ---------------------------------------
# Transform the Data Overall for Interview Progress calculation Across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by PSUs
group_by(DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01)
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate overall stats of PSUs
summarize(
tot_psu_n = n(),
comp_psu_n = sum(progress_psu_01)
) |>
# Calculate Percentage of Completed PSUs
mutate(comp_psu_pct = (comp_psu_n / tot_psu_n) * 100)
# Check Progress
data_progress_psu
# Check Progress
data_progress_dist
# ** District Completion Rate Table ---------------------------------------
# Transform the Data Overall for Interview Progress calculation Across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by PSUs
group_by(DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01)
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate Percentage of Completed PSUs
mutate(comp_psu_pct = (comp_hh_n / tot_hh_n) * 100)
# Check Progress
data_progress_dist
# ** District Completion Rate Table ---------------------------------------
# Transform the Data Overall for Interview Progress calculation Across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by PSUs
group_by(DATA04, DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01)
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate Percentage of Completed PSUs
mutate(comp_psu_pct = (comp_hh_n / tot_hh_n) * 100)
# Check Progress
data_progress_dist
# ** District Completion Rate Table ---------------------------------------
# Transform the Data Overall for Interview Progress calculation Across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by PSUs
group_by(DATA04, DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01),
.groups = "drop_last"
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate Percentage of Completed PSUs
mutate(comp_psu_pct = (comp_hh_n / tot_hh_n) * 100)
# Check Progress
data_progress_dist
# Check Progress
data_progress_dist |> view(title = "chk")
# ** District Completion Rate Table ---------------------------------------
# Transform the Data Overall for Interview Progress calculation Across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by PSUs
group_by(DATA04, DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01),
.groups = "drop_last"
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate Percentage of Completed PSUs
mutate(comp_psu_pct = (comp_hh_n / tot_hh_n) * 100) |>
# Calculate the Total and Completed PSUs across Districts
summarize(
tot_psu_n = n(),
comp_psu_n = sum(progress_psu_01),
tot_hh_in_dist_n = sum(tot_hh_n),
comp_hh_in_dist_n = sum(comp_hh_n)
)
# Check Progress
data_progress_dist |> view(title = "chk")
# ** District Completion Rate Table ---------------------------------------
# Transform the Data Overall for Interview Progress calculation Across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by PSUs
group_by(DATA04, DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01),
.groups = "drop_last"
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate the Total and Completed PSUs across Districts
summarize(
tot_psu_n = n(),
comp_psu_n = sum(progress_psu_01),
tot_hh_in_dist_n = sum(tot_hh_n),
comp_hh_in_dist_n = sum(comp_hh_n)
) |>
# Calculate the HH Completion Percentage
mutate(comp_hh_pct = comp_hh_in_dist_n / tot_hh_in_dist_n) |>
# Calculate the PSU Completion Percentage
mutate(comp_psu_pct = comp_psu_n / tot_psu_n, .after = comp_psu_n)
# Check Progress
data_progress_dist |> view(title = "chk")
# ** District Completion Rate Table ---------------------------------------
# Transform the Data Overall for Interview Progress calculation Across
# ALL DISTRICTS
df_tbl_progress_dist <- data_ex_01 |>
# Group by PSUs
group_by(DATA04, DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01),
.groups = "drop_last"
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate the Total and Completed PSUs across Districts
summarize(
tot_psu_n = n(),
comp_psu_n = sum(progress_psu_01),
tot_hh_in_dist_n = sum(tot_hh_n),
comp_hh_in_dist_n = sum(comp_hh_n)
) |>
# Calculate the HH Completion Percentage
mutate(comp_hh_pct = comp_hh_in_dist_n / tot_hh_in_dist_n) |>
# Calculate the PSU Completion Percentage
mutate(comp_psu_pct = comp_psu_n / tot_psu_n, .after = comp_psu_n)
# Check table
df_tbl_progress_dist |> view(title = "chk")
# View as gt table
df_tbl_progress_dist |>
gt() |>
cols_label(
DATA04 = "District Name",
tot_psu_n = "Total PSUs",
comp_psu_n = "Completed PSUs",
comp_psu_pct = "Completion Rate",
tot_hh_in_dist_n = "Total HHs",
comp_hh_in_dist_n = "Completed HHs",
comp_hh_pct = "Completion Rate"
) |>
gt_theme_espn() |>
opt_interactive(
use_pagination = FALSE
)
# View as gt table
df_tbl_progress_dist |>
gt() |>
fmt_percent(ends_with("pct")) |>
cols_label(
DATA04 = "District Name",
tot_psu_n = "Total PSUs",
comp_psu_n = "Completed PSUs",
comp_psu_pct = "Completion Rate",
tot_hh_in_dist_n = "Total HHs",
comp_hh_in_dist_n = "Completed HHs",
comp_hh_pct = "Completion Rate"
) |>
gt_theme_espn() |>
opt_interactive(
use_pagination = FALSE
)
# View as gt table
df_tbl_progress_dist |>
gt() |>
fmt_percent(ends_with("pct"), decimals = 1) |>
cols_label(
DATA04 = "District Name",
tot_psu_n = "Total PSUs",
comp_psu_n = "Completed PSUs",
comp_psu_pct = "Completion Rate",
tot_hh_in_dist_n = "Total HHs",
comp_hh_in_dist_n = "Completed HHs",
comp_hh_pct = "Completion Rate"
) |>
gt_theme_espn() |>
opt_interactive(
use_pagination = FALSE
)
# View as gt table
df_tbl_progress_dist |>
gt() |>
fmt_percent(ends_with("pct"), decimals = 1) |>
cols_label(
DATA04 = "District Name",
tot_psu_n = "Total PSUs",
comp_psu_n = "Completed PSUs",
comp_psu_pct = "Completion Rate of PSUs",
tot_hh_in_dist_n = "Total HHs",
comp_hh_in_dist_n = "Completed HHs",
comp_hh_pct = "Completion Rate of HHs"
) |>
gt_theme_espn() |>
opt_interactive(
use_pagination = FALSE
)
# Transform the Data Overall for Interview Progress calculation across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by Districts and PSUs
group_by(DATA04, DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01),
.groups = "drop_last"
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate the Total and Completed PSUs across Districts
summarize(
tot_psu_n = n(),
comp_psu_n = sum(progress_psu_01)
)
data_progress_dist
# Transform the Data Overall for Interview Progress calculation across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by Districts and PSUs
group_by(DATA04, DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01),
.groups = "drop_last"
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate the Total and Completed PSUs across Districts
summarize(
tot_psu_n = n(),
comp_psu_n = sum(progress_psu_01)
) |>
# Calculate Num of Districts Completed
mutate(progress_dist_01 = if_else(
tot_psu_n == comp_psu_n,
1, 0, missing = 0
))
data_progress_dist
# Transform the Data Overall for Interview Progress calculation across
# ALL DISTRICTS
data_progress_dist <- data_ex_01 |>
# Group by Districts and PSUs
group_by(DATA04, DATA02) |>
# Calculate the Total and Completed Cases across PSUs
summarize(
tot_hh_n = sum(cons),
comp_hh_n = sum(progress_all_01),
.groups = "drop_last"
) |>
# Calculate Number of PSUs Completed
mutate(progress_psu_01 = if_else(
comp_hh_n >= 30,
1, 0, missing = 0
)) |>
# Calculate the Total and Completed PSUs across Districts
summarize(
tot_psu_n = n(),
comp_psu_n = sum(progress_psu_01)
) |>
# Calculate Num of Districts Completed
mutate(progress_dist_01 = if_else(
tot_psu_n == comp_psu_n,
1, 0, missing = 0
)) |>
# Calculate overall stats of Districts
summarize(
tot_dist_n = n(),
comp_dist_n = sum(progress_dist_01)
) |>
# Calculate Percentage of Completed Districts
mutate(comp_dist_pct = (comp_dist_n / tot_dist_n)) |>
# Prepare the Progress Display Statistics in N (%)
mutate(stat_dist = str_c(
comp_dist_n,
" (",
scales::label_percent()(comp_dist_pct),
")"
))
data_progress_dist
# Check Progress
data_progress_dist
data_progress_dist$stat_dist
